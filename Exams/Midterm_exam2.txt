// Generated by Gemini
一、 Linux 与程序构建 (15%)

1. 写出一条完整的命令（可以使用 && 或分号）：在当前目录下创建一个名为 backup 的新文件夹，然后将当前目录下所有的 .cpp 文件移动到该文件夹中。

2. 下面的 Makefile 存在两处致命错误，请指出并改正：

Makefile
program: main.o utils.o
	g++ -c main.o utils.o -o program
main.o: main.cpp
	g++ main.cpp

二、 C++ 核心概念与 I/O (20%)

1. 简答题：请简述 int const * p1 和 int * const p2 的核心区别。（你可以尝试修改它们的值或指向，以此来举例说明）

2. 编程填空：给定一个字符串 string data = "100 89.5";，请使用输入字符串流 istringstream 将这两个数字分别读取到整型变量 score 和双精度浮点型变量 average 中。请写出实现该功能的核心代码。

三、 异常与函数指针 (15%)

1. 请写出一个函数指针 funcPtr 的声明语句。该指针指向的函数接受两个 double 类型的参数，并返回一个 bool 类型的值。

2. 以下异常处理代码的输出结果是什么？为什么？

C++
try {
    throw 3.14;
} catch (int e) {
    cout << "Caught Int" << endl;
} catch (double e) {
    cout << "Caught Double" << endl;
} catch (...) {
    cout << "Caught All" << endl;
}

四、 过程抽象与测试 (20%)

1. 请为以下函数编写完整的规范注释（Specification Comments），必须包含 // REQUIRES:, // MODIFIES:, 和 // EFFECTS:：

C++
void scale(int &value, int factor);
// 提示：该函数的功能是将传入的 value 乘以 factor，并将结果存回 value 中。不能传入负数的 factor。

2. 假设有一个函数 int find_min(const int arr[], int size); 用于寻找数组中的最小值（要求 size > 0）。请为它设计三组有代表性的测试用例数组和 size（分别对应 Typical, Boundary, Invalid 三种情况）。

五、 数据结构与递归编程 (30%)
注意：本部分要求使用纯递归（不能使用 for/while 循环），并注意检查空指针和 Base cases。

1. 二叉树求和：请编写一个函数 int tree_sum(node_t* tree)。它的功能是计算并返回这棵二叉树中所有节点的 value 的总和。如果树为空，请返回 0。

C++
int tree_sum(node_t* tree) {
  // 你的代码
}

2. 单向链表过滤：请编写一个函数 list_t list_remove_all(list_t list, int target)。它的功能是接收一个链表和一个目标值 target，返回一个新链表，这个新链表中删除了所有值等于 target 的元素。
限制：只能使用以下函数式接口：list_isEmpty(list_t), list_make(), list_first(list_t), list_rest(list_t), list_prepend(int, list_t)。

C++
list_t list_remove_all(list_t list, int target) {
  // 你的代码
}

Answer:
一、
1. Commands:
mkdir backup;
mv *.cpp backup;
2. (1) In the second line, delete "-c ".
   (2) In the fourth line, when compiling, the command should be "g++ -c main.cpp".

二、
1. "int const *p1" is a pointer to a constant integer. The address can be modified while the integer it points to
   cannot be modified.
   "int* const p2" is a const pointer to an integer. The address cannot be modified while the integer it points to
   can be modified.
2. Codes:
#include <sstream>
istringstream iss;
iss.str(data);
int score;
double average;
iss >> score >> average;

三、
1. bool (*funcPtr)(double, double);
2. Output: Caught Double
   Reason: In the try block, it throws a double type data 3.14, and it can be caught and handled with the catch
           block "catch (double e)".

四、
1. Comments:
// REQUIRES: factor >= 0
// MODIFIES: value
// EFFECTS: Multiply the input "value" with "factor", and store the result back to "value".
2. Typical: arr[] = {1, 2, 3}, size = 3
   Boundary: arr[] = {1}, size = 1
   Invalid: arr[] = {1, 2, 3}, size = -1

五、
1. Codes:
int tree_sum(node_t* tree) {
    if (tree == nullptr) {
        return 0;
    }
    int sum = 0;
    sum += tree_sum(tree->left) + tree_sum(tree->right) + tree->value;
    return sum;
}
2. Codes:
list_t list_remove_all(list_t list, int target) {
    if (list_isEmpty(list)) {
        return list;
    }
    if (list_first(list) == target) {
        return list_remove_all(list_rest(list), target);
    }
    return list_prepend(list_first(list), list_remove_all(list_rest(list), target));
}