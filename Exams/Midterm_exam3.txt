// Generated by Gemini
一、 Linux 与程序构建 (Linux & Build System) [15%]

1. (3分) 管道与重定向 (Pipes & Redirection): 写出一条完整的单行命令：在当前目录及所有子目录中查找所有的 .cpp 文件，筛选出其中包含单词 "TODO" 的行，并将这些行追加保存到当前目录下的 tasks.log 文件中。

2. (3分) 多命令串联 (Command Chaining): 写出一条单行命令（使用 ; 或 &&）：创建一个名为 build 的目录，进入该目录，创建一个名为 config.txt 的空文件，然后返回上级目录。

3. (9分) Makefile 纠错 (Makefile Debugging): 如下的 Makefile 旨在编译 main.cpp, utils.cpp 和 math.cpp 生成可执行文件 app。其中 main.cpp 和 utils.cpp 都包含了 utils.h。这个 Makefile 存在 三处致命错误或隐患（包含隐藏的依赖错误和 clean target 的问题）。请指出并修正。

Makefile
app: main.o utils.o math.o
	g++ -c main.o utils.o math.o -o app

main.o: main.cpp
	g++ main.cpp

utils.o: utils.cpp utils.h
	g++ -c utils.cpp

math.o: math.cpp
	g++ -c math.cpp

clean:
	rm app *.o

二、 C++ 核心概念与 I/O (C++ Concepts & I/O) [20%]

1. (5分) 字符串流 (istringstream): 给定一个字符串 string record = "Alice 92.5 88";，分别代表学生姓名、期中成绩和期末成绩。请写出 3-4 行核心代码，使用 <sstream> 中的 istringstream 将这三个数据提取到已声明好的变量 string name;, double mid_score;, int final_score; 中。

2. (6分) Const 限定符 (Const Pointers & References): 阅读以下代码，指出语句 A, B, C, D 中哪几个会导致编译错误，并一句话解释原因。

C++
int a = 10;
int b = 20;
const int* ptr1 = &a;
int* const ptr2 = &a;
const int& ref = a;

*ptr1 = 30;  // 语句 A
ptr1 = &b;   // 语句 B
ptr2 = &b;   // 语句 C
ref = 40;    // 语句 D

3. (4分) 引用与值类别 (Lvalue/Rvalue): 对于函数 int get_max(int x, int y);，以下两行代码能否编译通过？请简述原因。

C++
int& res1 = get_max(5, 10);       // 语句 1
const int& res2 = get_max(5, 10); // 语句 2

4. (5分) I/O 状态 (I/O State): 当你使用 cin >> my_int; 时，如果用户输入了字母 "abc"，cin 的内部状态标志位会发生什么变化？如果在这次错误输入后，你想继续读取下一个正常的输入，你必须调用 cin 的哪两个方法？

三、 异常与函数指针 (Exceptions & Function Pointers) [15%]

1. (5分) 复杂函数指针 (Function Pointers): 请写出一个声明语句，声明一个名为 processor 的函数指针。该指针指向的函数：接收一个 int 数组和一个 int（代表数组大小）作为参数，并返回一个指向 double 的指针。

2. (10分) 异常匹配陷阱 (Exception Matching Traps): 阅读以下代码。请问程序运行后会输出什么？为什么？（重点考察隐式类型转换在 catch 块中的适用性）。

C++
#include <iostream>
using namespace std;

int main() {
    try {
        float f = 5.5f;
        throw f;
    } 
    catch (int e) {
        cout << "Caught int: " << e << endl;
    } 
    catch (double e) {
        cout << "Caught double: " << e << endl;
    } 
    catch (...) {
        cout << "Caught something else" << endl;
    }
    return 0;
}

四、 过程抽象与测试 (Procedural Abstraction & Testing) [20%]

1. (8分) 规范注释 (RME Specs): 请为下面的函数编写完整的 Specification Comments (// REQUIRES:, // MODIFIES:, // EFFECTS:)。（注意避开遗漏 MODIFIES 的易错点）

C++
// 提示：该函数将传入的 new_score 添加到总分 total_score 中，
// 并将 update_count 加 1。分数必须大于等于 0。
void add_score(int new_score, int& total_score, int& update_count);

2. (12分) 边界测试设计 (Testing Domains): 假设有一个函数用于计算网购运费：int calc_shipping(int weight, int distance);。
// REQUIRES: weight > 0, distance >= 0
请根据增量测试原则，设计 3 组测试用例（提供具体的 weight 和 distance 的值）：

Typical (常规情况)

Boundary (边界情况)

Invalid (非法情况 —— 要求必须是违背 Requires 逻辑的情况，不能是类型错误如传入字符串)。

五、 数据结构与递归编程 (Data Structures & Recursion) [30%]

注意：本部分要求完全不使用任何形式的循环（for, while, goto），只能使用纯递归实现。注意检查 Base cases 以及空指针。

题 A：二叉树的严格递增路径判定 (12分)
编写一个纯递归函数 bool is_strict_path(node_t* tree)。如果这棵二叉树中，任意一个节点（只要它有子节点）的 value 都严格小于其所有直接子节点（左孩子和右孩子）的 value，则返回 true。如果树为空，也返回 true。（注意区分指针判空与 value 比较）

C++
struct node_t {
    int value;
    node_t* left;
    node_t* right;
};

bool is_strict_path(node_t* tree) {
    // 你的代码
}
题 B：单向链表的纯递归插入 (18分)
编写一个纯递归函数 list_t list_insert_after(list_t list, int target, int val)。它的功能是：在链表中每一次出现 target 值的地方，在它的紧接着的后面插入一个新节点，值为 val。返回修改后的新链表。
限制：只能使用提供的函数式接口 list_isEmpty(list_t), list_make(), list_first(list_t), list_rest(list_t), list_prepend(int, list_t)。
提示：切记你的肌肉记忆：遇到需要保留当前节点继续处理的情况，应如何嵌套 list_prepend 和递归调用？

C++
list_t list_insert_after(list_t list, int target, int val) {
    // 你的代码
}

Answer:
一、
1. Commands:
grep -r "TODO" *.cpp >> tasks.log (此题超纲)
2. Commands:
mkdir build;
cd build;
touch config.txt;
cd ..;
3. (1) In the second line, delete "-c ".
   (2) In the fourth line, when compiling, the command should be "g++ -c main.cpp".
   (3) Before the ninth line, add the command ".PHNOY clean:".

二、
1. Codes:
istringstream iss;
iss.str(record);
string name;
double mid_score;
int final_score;
iss >> name >> mid_score >> final_score;
2. A is wrong. ptr1 is a pointer to a constant integer. The value of the integer cannot be modified.
   C is wrong. ptr2 is a const pointer to an integer. The address cannot be modified.
   D is wrong. ref is a reference of a constant integer. The value of the integer cannot be modified.
3. (1) Unsuccessful compiling. An "int&" cannot be initialized with a rvalue.
   (2) Successful compiling. When adding a "const" before "int&", it can be initialized with a rvalue.
4. (1) The stream will enter a failed state.
   (2) "cin.clear()" and "cin.ignore()". (此题超纲)

三、
1. double* (*processor)(int*, int);
2. Output: Caught something else
   Reason: The try block throws a float variable, which cannot be handled with catch blocks requiring int or double
           input. Instead, it will handled with the default catch block "catch (...)" at the end.

四、
1. Comments:
// REQUIRES: new_score >= 0, total_score >= 0
// MODIFIES: total_score, update_count
// EFFECTS: Add the value of the input "new_score" to "total_score" and increment "update_count" by 1.
2. Typical: weight = 2, distance = 1
   Boundary: weight = 2, distance = 0
   Invalid: weight = 2, distance = -1

五、
1. Codes:
bool is_strict_path(node_t* tree) {
    if (tree == nullptr) {
        return true;
    }
    if ((tree->left == nullptr) && (tree->right == nullptr)) {
        return true;
    } else if ((tree->left != nullptr) && (tree->right == nullptr)) {
        if (tree->value >= tree->left->value) {
            return false;
        }
        return is_strict_path(tree->left);
    } else if ((tree->left == nullptr) && (tree->right != nullptr)) {
        if (tree->value >= tree->right->value) {
            return false;
        }
        return is_strict_path(tree->right);
    } else {
        if ((tree->value >= tree->left->value) || (tree->value >= tree->right->value)) {
            return false;
        }
        return is_strict_path(tree->left) && is_strict_path(tree->right);
    }
}
2. Codes:
list_t list_insert_after(list_t list, int target, int val) {
    if (list_isEmpty(list)) {
        return list;
    }
    if (list_first(list) == target) {
        return list_prepend(list_first(list), list_prepend(val, list_insert_after(list_rest(list), target, val)));
    }
    return list_prepend(list_first(list), list_insert_after(list_rest(list), target, val));
}