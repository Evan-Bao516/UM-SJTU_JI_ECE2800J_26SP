// Generated by Gemini
一、 高级 Linux 构建与环境 (Advanced Linux & Build System) [20分]
1. (3分) 管道与复杂重定向:
写出一条完整的单行命令：在当前目录（包含所有子目录）寻找所有 .h 头文件，找出其中包含 "class" 关键字的行，并将这些行追加到父目录（上一级目录）的 class_list.txt 文件中，同时忽略所有报错信息（将 stderr 重定向到 /dev/null）。

2. (3分) 目录与权限:
写出一条命令（或用 ; / && 串联）：强制删除当前目录下名为 build_cache 的文件夹（即便里面有只读文件），然后退回用户的主目录 (Home directory)，并列出主目录下的所有隐藏文件。

3. (10分) 终极 Makefile 纠错:
下面的 Makefile 引入了宏变量，旨在编译一个复杂的程序。其中隐藏了 5 处 致命错误或极不规范的隐患（包含了你以前踩过的坑）。请逐一指出并给出正确的写法。

Makefile
CXX = g++
FLAGS = -Wall -Werror

all: system

system: main.o parser.o auth.o
	$(CXX) main.o parser.o auth.o

main.o: main.cpp parser.h auth.h
	$(CXX) $(FLAGS) main.cpp

parser.o: parser.cpp
	$(CXX) -c parser.cpp

auth.o: auth.cpp auth.h
	$(CXX) -c auth.cpp

clean:
	rm system *.o

4. (4分) 编译原理:
当你运行 g++ -c main.cpp 后，生成的文件是什么类型的？如果在这一步报错 "undefined reference to foo()"（未定义的引用），这可能吗？为什么？

二、 C++ 核心概念、指针与 I/O 深度解析 (Deep C++ Concepts & I/O) [25分]
1. (6分) 多级指针与内存追踪:
仔细阅读以下代码，请问在代码执行完毕后，变量 a, b, c 的值分别是什么？

C++
int a = 10, b = 20, c = 30;
int* p1 = &a;
int* p2 = &b;
int** p3 = &p1;

*p3 = p2; 
**p3 = 40; 
p3 = &p2; 
*p3 = &c;
**p3 = 50;

2. (5分) I/O 状态恢复与高级解析:
假设你需要使用 cin 读取一个整数。由于用户恶意捣乱，输入了 "ERROR 404"。
请写出 3~4 行代码：处理这种失败状态，清空错误标志，并跳过输入缓冲区中的所有残留字符（直到遇到换行符 \n），以便准备下一次干净的读取。

3. (6分) Const 与引用的极限拉扯:
判断以下 A、B、C 三个语句是否能通过编译，并简述理由。

C++
int x = 5;
const int y = 10;

int& const ref1 = x;         // 语句 A
const int& ref2 = y;         // 语句 B
const int* const & ref3 = &x; // 语句 C

4. (8分) 字符串流的混合解析:
给定一个复杂字符串 string log = "[INFO] ID:1042 Score:98.5";。
请使用 istringstream 配合合适的提取符（>>）或忽略函数（如 ignore），提取出其中的整数 1042（存入 int id）和浮点数 98.5（存入 double score）。请写出核心解析代码。

三、 异常流与函数指针进阶 (Exceptions & Function Pointers) [15分]
1. (5分) 噩梦级函数指针:
声明一个名为 core_handlers 的大小为 5 的数组，数组里存放的是函数指针。
这些指针指向的函数：接收一个 const char* 和一个 int，并返回一个 void*。

2. (10分) 异常的嵌套与抛出:
阅读以下复杂的 try-catch 嵌套代码，写出终端的完整输出结果，并简述抛出和捕获的流转过程。

C++
void risky_func() {
    try {
        cout << "Step 1" << endl;
        throw 2.5; // 抛出 double
    } catch (int e) {
        cout << "Caught Int in risky: " << e << endl;
    }
    cout << "Step 2" << endl;
}

int main() {
    try {
        cout << "Main Start" << endl;
        risky_func();
        cout << "Main Mid" << endl;
    } catch (double e) {
        cout << "Caught Double in main: " << e << endl;
    } catch (...) {
        cout << "Caught Unknown" << endl;
    }
    cout << "Main End" << endl;
    return 0;
}

四、 过程抽象、断言与鲁棒测试 (Abstraction & Testing) [15分]
1. (6分) RME 规范补全:
为一个“交换两个指针所指向的地址”的函数写完整的 Specification Comments。

C++
// REQUIRES: ______________
// MODIFIES: ______________
// EFFECTS:  ______________
void swap_pointers(int** ptr1, int** ptr2);

2. (9分) 边界与非法测试极限设计:
有一个用于注册系统的高级函数：
bool register_user(int age, int passcode_length);
// REQUIRES: age >= 18, 6 <= passcode_length <= 12
请设计 3 组 Typical, 3 组 Boundary（必须卡在最极限的边缘）, 3 组 Invalid（逻辑越界）用例。明确写出每组的 age 和 passcode_length。

五、 数据结构与纯递归编程挑战 (The Recursion Boss Fight) [45分]
终极警告：本部分绝对禁止使用 for, while, goto 或任何形式的迭代。只能使用递归。你可以自由编写辅助函数（Helper Functions），但辅助函数也必须是纯递归的！

题 A：二叉树的“最大路径和” (15分)
给定一棵二叉树 node_t* tree（节点有负数）。编写一个递归函数 int max_leaf_path(node_t* tree)。
它的功能是：从根节点走到任意一个叶子节点（没有子节点的节点），计算经过的所有节点值的和。返回这些可能路径中最大的那个和。
如果树为空，返回 0。
(提示：你需要区分只有左孩子、只有右孩子、和同时有左右孩子的情况，以确保必须走到叶子节点)。

C++
int max_leaf_path(node_t* tree) {
    // 你的代码
}

题 B：函数式单向链表 - “去重” (15分)
编写纯递归函数 list_t list_deduplicate(list_t list)。
给定一个已经升序排好的链表，去除所有相邻的重复元素，只保留一个。
例如：输入 [1, 1, 2, 3, 3, 3, 4]，返回 [1, 2, 3, 4]。
限制：仅能使用 list_isEmpty, list_make, list_first, list_rest, list_prepend。

C++
list_t list_deduplicate(list_t list) {
    // 你的代码
}

题 C：函数式单向链表 - “彻底反转” (15分)
编写纯递归函数 list_t list_reverse(list_t list)，返回一个与原链表元素顺序完全颠倒的新链表。
例如：输入 [1, 2, 3]，返回 [3, 2, 1]。
限制：你只能使用上面提过的 list API。强烈提示：你需要自己设计一个带“累加器(Accumulator)”的辅助递归函数，否则用现有 API 很难做到 O(N) 复杂度。这也是函数式编程中最经典的递归模式。

C++
// 你的辅助函数 (自拟)

// 主调函数
list_t list_reverse(list_t list) {
    // 你的代码
}

一、
1. grep -r "class" *.h >> ../class_list.txt (此题超纲)
2. Commands:
rm -rf build_cache;
cd ~;
ls -a ~;
3. (1) In the fifth line, when linking, the command should be "g++ -o system main.o parser.o auth.o".
   (2) In the seventh line, when compiling, the command should be "g++ -Wall -Werror -c main.cpp".
   (3) In the eighth line, add "parser.h".
   (4) Before the twelfth line, add ".PHONY". (此题超纲)
   (5) In the tenth line, when compiling, the command should be "g++ -Wall -Werror -c main.cpp".
4. (1) It generates a .o file.
   (2) It is impossible because it only compiles instead of linking, and it does not check whether foo()
       is defined.

二、
1. a = 10, b = 40, c = 50
2. Codes: (此题超纲)
cin.clear();
cin.ignore();
cin.get();
3. A is wrong. "int& const" is invalid.
   B is right. ref2 is a reference to the constant integer y.
   C is right. ref3 is a const reference of a pointer to a constant integer. It can point to x.
4. Codes: (此题超纲)
istringstream iss;
iss.str(log);
int id;
double score;
iss.ignore(10);
iss >> id;
iss.ignore(7);
iss >> score;

三、
1. void* (*core_handlers[5])(const char*, int); (此题超纲)
2. Output: Main start
           Step 1
           Caught Double in main: 2.5
           Main end
   Process: Start from the try block of main -> Go into the try block of risky_func() -> 2.5 thrown and cannot be
            caught by the catch block of risky_func() -> 2.5 caught by the catch (double e) block of main -> end

四、
1. Comments:
// REQUIRES: ptr1 and ptr2 are valid, non-null pointers to int* objects.
// MODIFIES: The int* objects pointed to by ptr1 and ptr2.
// EFFECTS:  Exchanges the values (memory addresses) stored in the two int* objects pointed to by ptr1 and ptr2. 
//           After the call, *ptr1 holds the original value of *ptr2, and *ptr2 holds the original value of *ptr1.
2. Typical: age = 20, passcode_length = 8; age = 21, passcode_length = 9; age = 22, passcode_length = 10
   Boundary: age = 18, passcode_length = 8; age = 18, passcode_length = 6; age = 18, passcode_length = 12
   Invalid: age = -1, passcode_length = -1; age = -2, passcode_length = -2; age = -3, passcode_length = -3; 

五、
1. Codes:
int max_leaf_path(node_t* tree) {
    if (tree == nullptr) {
        return 0;
    }
    int sum = 0;
    if ((tree->left == nullptr) && (tree->right == nullptr)) {
        sum += tree->value;
        return sum;
    } else if ((tree->left != nullptr) && (tree->right == nullptr)) {
        sum += tree->value;
        return sum + max_leaf_path(tree->left);
    } else if ((tree->left == nullptr) && (tree->right != nullptr)) {
        sum += tree->value;
        return sum + max_leaf_path(tree->right);
    } else {
        sum += tree->value;
        int left_max = max_leaf_path(tree->left);
        int right_max = max_leaf_path(tree->right);
        if (left_max <= right_max) {
            return sum + right_max;
        }
        return sum + left_max;
    }
}
2. Codes:
list_t list_deduplicate(list_t list) {
    if (list_isEmpty(list) || list_isEmpty(list_rest(list))) {
        return list;
    }
    if (list_first(list) == list_first(list_rest(list))) {
        return list_deduplicate(list_rest(list));
    }
    return list_prepend(list_first(list), list_deduplicate(list_rest(list)));
}
3. Codes:
list_t reverse_helper(list_t list, list_t new_list) {
    if (list_isEmpty(list)) {
        return new_list;
    }
    new_list = list_prepend(list_first(list), new_list);
    return reverse_helper(list_rest(list), new_list);
}

list_t list_reverse(list_t list) {
    list_t new_list = list_make();
    return reverse_helper(list, new_list);
}